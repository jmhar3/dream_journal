<% if @notes %>
    <ul>
        <% @notes.each do |note| %>
            <li class="row">
                <%= note.name %>
                <%= link_to '💌', edit_note_path(note) %>
            </li>
        <% end %>
    </ul>
<% end %>