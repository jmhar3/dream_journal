<% if goals %>
    <ul>
        <% goals.each do |goal| %>
            <li class="row">
                <%= goal.name %>
                <%= link_to '💌', edit_goal_path(goal) %>
            </li>
        <% end %>
    </ul>
<% end %>